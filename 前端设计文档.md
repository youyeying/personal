# 个人记录系统 前端设计文档

> 版本：v1.27.0
> 技术栈：Vue3 + TypeScript + Vite 8 + Pinia + Vue Router + Element Plus（全量）+ SCSS
> dev 端口：5173 ｜ 后端：http://localhost:8080
> 构建：`npm run build` = `vue-tsc --noEmit && vite build`（仅 `vite.config.ts` 单配置文件，不产 ts 产物）
>
> 相关文档：后端设计文档 → `E:/personal/后端设计文档.md` ｜ 数据库设计文档 → `E:/personal/database/数据库设计文档.md`

---

## 1. 项目结构

```
front/
├── index.html                # HTML 入口
├── vite.config.ts            # Vite 配置（@ 别名 / 代理，唯一配置，proxy 到 8080）
├── tsconfig.json             # TS 配置（noEmit，`vue-tsc --noEmit` 类型检查）
└── src/
    ├── main.ts               # 应用入口：挂载/路由/状态/Element Plus/暗色 css
    ├── App.vue               # 根组件（<router-view/>）
    ├── styles/               # 全局样式
    │   ├── theme.scss        # 主题令牌 CSS 变量（--cb-*）+ 明暗主题
    │   ├── base.scss         # 基础（字体、卡片、工具类 .num / .card / .flex-between）
    │   └── element.scss      # Element Plus 主题覆盖
    ├── router/index.ts       # 路由 + 登录守卫
    ├── store/
    │   ├── theme.ts          # 模块主题（MODULES、ModuleTheme、useThemeStore）
    │   ├── layout.ts         # 布局状态（导航宽度/伸缩/抽屉/断点）
    │   └── user.ts           # 用户状态（token / userInfo）
    ├── api/                  # 请求封装 + 各模块接口
    │   ├── request.ts        # requestApi（封装 axios，统一 token/响应/错误）
    │   ├── auth.ts           # 登录/注册/当前用户
    │   ├── expense.ts / health.ts / learn.ts / dev.ts / dailyNote.ts / shift.ts / operationLog.ts
    ├── utils/                # 工具
    │   ├── format.ts         # 时间格式化（formatDate / formatDateTime / formatShortTime）
    │   └── validators.ts     # 表单校验
    ├── views/                # 页面（路由对应）
    │   ├── login/            # 登录页
    │   ├── configuration/    # 布局区（非路由页面）：SideNav / AppHeader / AppLogo / ThemeToggle
    │   ├── home/             # 布局壳 Home.vue（header + 导航 + router-view）
    │   ├── homepage/         # 首页概览 Overview.vue
    │   └── expense|health|learn|daily-note|operation-log|dev-log|profile/  # 各模块页
    ├── components/           # 公共组件（按组件建目录，父组件传参）
    │   ├── DataList/         # 泛型列表（列模式/表头/点行/居中）
    │   ├── PagePanel/        # 页面模板（header卡+数据卡+不透明加载遮罩）
    │   ├── PagePager/        # 分页条（el-pagination 统一配置，双向绑定 page/size）
    │   ├── LoadingMask/      # 不透明加载遮罩
    │   ├── SubNav/           # 内容区子页 Tab 页头
    │   ├── RangeTabs/        # 范围切换按钮组
    │   ├── CollapseBox/      # 收纳/展开动画容器（expand-end 触发图表 resize）
    │   ├── MetricCard/       # 指标卡（label/tone/默认插槽+sub 插槽）
    │   ├── BaseButton/  # 通用按钮
    │   ├── FloatingInput/    # 浮动标签下划线输入
    │   ├── ModulePlaceholder/# 模块占位页
    │   └── loading/          # 加载动画
    └── config.ts             # 应用名 / 版本号 / 版本规则注释
```

**目录规范（用户约定）**：
- `views/` 只放页面；`components/` 只放可复用公共组件（按 `components/<组件名>/` 建目录，含 `.vue` 与同名 `.scss`）
- 布局组件在 `views/configuration/`（SideNav / AppHeader 等，随布局一处维护）
- `.vue` **不写任何 `<style>` 内联样式内容**（哪怕一个类、一个公共组件）：样式一律放**平级独立 `.scss` 文件**（页面 `views/x/x.scss`、组件 `components/X/x.scss`），由 `<style lang="scss" scoped>@use './xxx'</style>`（或全局组件 `@use` 引入）；2026-08-22 排查 `PagePager/LoadingMask` 内联样式已外置
- 颜色一律用主题令牌 `--cb-*`，禁止硬编码；数字/时间用等宽 `.num`
- **公共组件强约定（2026-08-22）**：公共组件必须能**跨项目复用**——不引用 `views/`/`store/`/`api/` 等项目专属属性，业务数据一律由父组件 props/插槽传入；公共组件之间可互相引用、可用 `utils/` 工具类与 `--cb-*` 设计令牌

---

## 2. 主题与设计语言

### 2.1 主题令牌（theme.scss 的 CSS 变量，`--cb-*`）
- 主色 `--cb-mod`（随模块切换）；画布 `--cb-canvas`；表面 `--cb-surface`；墨色 `--cb-ink` / `--cb-ink-secondary` / `--cb-ink-muted`；语义色 `--cb-success` / `--cb-warning` / `--cb-error`；线 `--cb-hairline`；圆角 `--cb-radius-*`；间距 `--cb-space-*`；字号 `--cb-font-*`
- 暖色系：画布暖米白 `#f2ede4`、主色陶土棕 `#a8765a`、文本墨绿灰 `#2c312f`
- 明暗主题：`<html>.dark` 下全局令牌切换；入口引入 `element-plus/theme-chalk/dark/css-vars.css`；暗色主色提亮 `#c08563`、画布 `#1d1b17`

### 2.2 模块主色（theme.ts MODULES）
| 模块 | 路由 | 主色 |
| --- | --- | --- |
| 首页概览 | / | #a8765a 陶土棕 |
| 记账 | /expense | #c08a3e 暖琥珀 |
| 健康 | /health | #3f7a72 墨茶绿 |
| 学习 | /learn | #4f7a8c 雾磁蓝 |
| 每日总结 | /daily-note | #a8716a 烟玫瑰 |
| 操作日志 | /operation-log | #6b7a66 橄榄 |
| 开发日志 | /dev-log | #5f7a8c 幽蓝 |
| 个人中心 | /profile | #8a8a80 云雾灰 |

切换路由时导航栏/header/内容区主色平滑过渡到该模块主题色。

### 2.3 设计偏好（用户强约定）
- 简约现代中后台；**避开**：紫色渐变、通用 AI 模板、过度悬浮卡、超大圆角、胶囊按钮、高字重标题
- 主行动色单一；风险/业务状态用克制语义色；图标用 SVG/Element 图标，不用 emoji 代替功能图标（心情等场景用户允许 emoji）
- 全局 `user-select:none`，输入框放行；编号/时间/版本用等宽字体

---

## 3. 布局（views/configuration + home）

- **导航宽度** `NAV_WIDTH = 280`（用户最终确认）；**缩导航栏**=仅图标（76px）、含 el-tooltip
- **导航三态**：≥1280 完整；<1280 自动收缩；<600 自动隐藏为全屏抽屉（drawer Transition）
- **左上角专属 Logo（AppLogo）**：随路由模块变化，为各模块独立徽标；小于 0.75 倍缩放；点击 Logo 逻辑：<600 开全屏抽屉 / <1280 切隐藏恢复 / ≥1280 手动收缩时展开（完整展开时无操作）
- **header 高度 68px**，背景随模块主色淡染 + 底部 2px 主色描边；右上角头像壳+昵称 + `el-dropdown`（个人中心/退出登录）
- **底部（SideNav foot）固定**：白天黑夜开关（上）+ 版本号（下）；导航隐藏时开关移到 header 头像左边
- 布局与内容各自独立滚动；内容区 `.app-layout__main` 加 `scrollbar-gutter: stable` 固定滚动条占位，避免内容高度变化导致宽度跳动

---

## 4. 公共组件（components/，父组件传参）

| 组件 | 说明 |
| --- | --- |
| DataList | 泛型列表面板：`columns` 列定义（label + ratio/width，**width 支持 number→px 或 string→原样注入 grid 如 `'calc(11ch + 2*var(--cb-space-3) + 4px)'` 随字号缩放**）+ 表头；`clickable`+`row-click`；文本居中；固定高度（默认 15 行×40）、仅数据行有边框、空行无边框、无滚动、超长省略；不传 columns 回退默认插槽。**v1.23.0 响应式列隐藏**：列支持 `hideBelow`（px）→ 视口小于阈值时该列不渲染（次要列让位，完整数据看行弹窗），配合 `useWindowWidth` 监听视口宽度。**v1.24.0 卡片降级**：新增 `cardBelow`（px）→ **容器宽度**（ResizeObserver 测组件根宽，非视口）小于阈值时整表降级为堆叠卡片（按列定义自动组织：首个固定列=卡片头放大，其余非 ops 列=label→value 对，`ops:true` 或 key=`ops` 列放入卡片底部，保留 `cell` 插槽渲染与整卡点击 `row-click`）；卡片模式忽略 `hideBelow`（数据全展示）；未传 `cardBelow` 则不启用卡片化 |
| DateRangePicker | 日期范围选择（v1.23.0 抽离公共组件）：统一 value-format=YYYY-MM-DD / 分隔符 / 中文 placeholder；**宽屏 type="daterange" 双日历面板**，**窄屏（视口<620px 放不下 646px 双面板）自动退化为上下两个单日选择**；`popper-options.preventOverflow`（boundary=viewport + fallbackPlacements）杜绝右端溢出；输入框 `flex:0 0 auto` 防被 flex 容器拉伸；事件 `update:modelValue`/`change` 双向透传 |
| PagePanel | 页面模板：header 卡（标题 + slot `header`）+ 数据卡（默认插槽，高度自适应无内滚动）；`loading` 时数据卡显示不透明遮罩 |
| ModulePlaceholder | 模块占位页（title/desc/icon 可传，默认当前模块） |
| AppLogo | 专属 Logo（kind prop） |
| ThemeToggle | 白天黑夜苹果风开关（58×32） |
| InlineLoading | 区域内加载（卡片/按钮/图标），`mode` 三态：spinner 纯转圈 / text 纯文字 / spinner-text 转圈+文字 |
| FullScreenLoading | 全屏不透明加载，可选自动淡出（`duration`）；`mode` 三态同上 |
| BlockTitle | 区块标题（v1.17.0）：`title` + `hint` + 具名插槽 `aside`（右侧操作区靠右）；替代各模块手写 `__block-title` |
| EmptyState | 空态（v1.17.0）：具名插槽 `icon`（可选，不传不画圆底）+ `text` + `actionText`（引导按钮，emit `action`）；统一各模块空态 |

**强约定**：公共组件一律数据/内容由父组件传入，不直接写业务内容。

**加载组件统一约定（mode 三态）**
- `mode='spinner'`：仅转圈（无文字）
- `mode='text'`：仅文字（转圈隐藏）
- `mode='spinner-text'`：转圈 + 文字（默认）
- `PagePanel` 透传 `loadingMode`；`BaseButton` 图标区用 `mode="spinner"`

---

## 5. 已实现页面

### 5.1 记账 `/expense`（组合层 `views/expense/ExpenseList.vue`）
> v1.16.0 起按登录式架构组件化拆分（仅结构拆分）：组合层 `ExpenseList.vue`（SubNav + activeSub + 分类一次性加载 + tick 跨页刷新协调）+ `components/ExpenseInput`(记一笔)/`ExpenseOverview`(概览)/`ExpenseDetail`(明细+修改弹窗)；分类图标映射抽 `utils/category.ts`(`catIcon`)；任一子页数据变更 `emit('changed')`→tick++→三处联动重载（等价原 refreshAll）；概览趋势点柱子 `emit('select-date')`→父级 jumpDate→明细联动
- 布局：**内容区页头三子页**（`exp__subnav` Tab，v-show 切换）——**记一笔 / 概览 / 明细**，`max-width:1500px` 居中
- **图标体系（v1.11.0 起）**：功能图标全部用 **Lucide SVG**（`@lucide/vue` 本地依赖，开源可免费下载）；分类映射 `Utensils/Car/ShoppingBag/House/Gamepad2/Pill/BookOpen/Gift/Briefcase/Laptop/TrendingUp`（未匹配回退 `MoreHorizontal`），收纳按钮 `ChevronUp/ChevronDown`，明细/最近记录行首 28px 圆形图标底；禁止 emoji 代替功能图标
- **记一笔子页**：支出/收入分段切换 → 分类宫格（Lucide 图标+名称，`getCategories` 按 sortOrder 排序）→ 大号等宽金额输入 + 日期（默认今天可补记）+ 备注 → 保存（清空金额保留分类可连续记账）+ **最近记录速览**（最新 5 条，独立轻量加载 `recentLoading`，可跳明细页）
- **概览子页**：标题/指标随范围动态（7天→本周概览、30天→本月概览、全部→全部概览，指标标签同步）+ **ECharts 横向条形趋势图**（日期左轴竖排 MM-DD 短标签、金额横轴；**高度按天数自适应**；7/30/全部 切换，hover tooltip 高亮）+ **可收纳/展开（带动画，grid-template-rows 0fr↔1fr）**；下方**支出/收入构成卡**（ECharts 环形图 + 分类排行，复用概览统计数据）
- **明细子页**：类型/分类/日期筛选 + 按日分组列表（当日小计、行首 Lucide 图标）+ **修改弹窗**（v1.14.0：支出/收入分段 + 分类下拉[随类型过滤] + 金额 + 日期/备注，改后联动刷新明细/概览/最近记录）+ 删除 + 分页每页 15/30/50；**点趋势柱子联动到此页某天**（注：因「按日分组」两级表头结构，明细未用 DataList，保留自定义）
- **黑夜模式适配**：ECharts canvas 不解析 CSS 变量，图表颜色经 `utils/theme.ts` 的 `cssVar()` 取实际色值；`watchTheme()` 在 dark 切换时重绘图表
- **加载分组（不透明遮罩，同源合用/异源独立）**：分类 `catLoading` 独立；概览（指标+趋势+构成）`overviewLoading` 共用一个；明细 `listLoading` 独立；最近记录 `recentLoading` 独立
- **ECharts 告警防护**：概览/构成是子页 Tab（初始 display:none），`renderChart`/`renderDonut` 均 `clientWidth>0` 才 init；`watch(activeSub)` 切到概览时 resize/补渲染
- 新增 `api/expense.ts`（分类/分页/统计/增删改）+ `utils/format.ts` 的 `formatMoney`
- `/expense/stat`（ExpenseStat.vue）暂为占位

### 5.2 每日总结 `/daily-note`（组合层 `views/daily-note/DailyNote.vue`）
> v1.17.0 起组件化拆分（保持单页不加 Tab）：组合层 `DailyNote.vue`（持有班期/班表/总结数据、选中日期、汇总、班表草稿、加载与全部逻辑）+ `components/DailyCalendar`(日历) / `DailySummaryCard`(小汇总) / `DailyEdit`(心情+小结，自持编辑态) / `DailyTimeline`(时间线) / `ShiftEditorDialog`(班表弹窗) + `dailyNoteShared.ts`(MOODS/WEEK_LABELS/moodLabel/weekdayOf/cycleStartOf/cycleEndOf)；编辑态 `emit('save',{mood,content})` 由组合层提交
- 班期视图：**21号 → 次月20号**（动态 `cycleEndOf`，跨月正确），可切上/下班期
- 日历列数**随容器宽度自适应 7→6→5→4→3**（`DailyNote.vue` 用 `ResizeObserver` 监听容器宽度，断点 ≥1040/900/760/620 对应 7/6/5/4 列、否则 3 列，注入 `grid-template-columns`；表头与日期共用列数）；内容区 `max-width:1200px` 居中 + `min-width:520px`
- 日历格：班次短标签着色（`SHIFT_META` 内联色，标签统一 HH:mm 格式如 `07:30-16:30`、跨天 `17:00-02:00`）+ 心情 emoji + 选中高亮
- 当日小汇总卡（`min-height` 保持高度稳定）：支出/收入/体重(▲▼变化)/学习/开发
- 心情预设（emoji）+ 今日小结 + 保存；班表批量编辑弹窗：**导入 xlsx 班表文件**（第1行日期、第2行班次代码 P7A1/P7C1/P10A1/P12A1/P15A1/P17A1/P23A1/休，`api/shift.ts` 的 `parseShiftFile` 解析并合并，未命中日期保留原草稿）+ 逐日手选
- 本班期时间线（倒序），点击跳转该日

### 5.3 开发日志 `/dev-log`（views/dev-log/DevLog.vue）
> v1.19.0 起：支持一天多段会话（开始/结束可多次，今日时长=各段累计）；header 精简（搜索下移到列表区）；新增「今日开发时段」卡；结束开发弹窗批量录入功能。
- **header**：会话状态徽标（开发进行中/今日未开发）+ 日期范围筛选 + 开始开发/结束开发按钮（标题展示今日累计时长）
- **今日开发时段卡**（独立，数据卡内顶部）：当天多段会话 `summary.sessions`，第 N 段 起止 `HH:mm`（`hhmm()` 取字符串第 11-16 位）/ 时长（进行中段显示「进行中」）/ 状态标签（进行中-绿 / 已结束-灰）
- **列表区**：顶部**内容模糊搜索**（客户端过滤，变更回第 1 页）+ DataList 列模式（日期/类型/模块/内容）——**日期132/类型60/模块104 固定宽完整显示 + 仅内容列省略（v1.24.0 起去除 hideBelow，三个字段不再隐藏）+ 列表区固定 `font-size:14px`（v1.20.3，避免 `.num` 继承响应式 --cb-font-md 大屏放大导致日期/模块省略）**；列表容器 `min-width:550px`；点击行弹详情；分页 15/30/50（`:max-rows="size"` 联动）
- **结束开发弹窗**：多条收集（类型下拉[新增/修改/删除/修复] + 模块下拉[filterable allow-create 可自定义] + 描述[textarea 多行] + 添加/删除行）→ 先逐条 `POST /api/dev/features`（空描述跳过）→ 再 `POST /api/dev/session/end` → **`await load()` 刷新列表（v1.20.3 修复：原未 await 且被外层 catch 吞掉导致结束开发后列表不刷新）**；底部 `共 N 条` + 主按钮（无有效行时显示「直接结束开发」）
- **模块清单（v1.20.2 统一，写 md 必用）**：`记账/健康/学习/每日总结/开发日志/操作日志/个人中心/首页概览/认证/安全/布局/通用/系统/文档/前端/后端`。规则（用户确认）：页面改动归对应页面模块（前后端都算该模块）；**前端公共组件/工具→前端、前端样式/主题/字体/布局→布局**、后端公共工具→后端；前后端数据验证→安全；数据库→后端；文档→文档；工程/部署→系统；跨模块业务兜底→通用。历史 feature_log 已遍历修正 30 条归类（系统→业务模块、组件/通用→前端、样式类→布局）。
- **导入 md 文件（v1.20.0）**：弹窗内「导入 md 文件」`el-upload`（accept .md/.markdown）→ `FileReader` 读取 → `utils/mdDraft.ts` 的 `parseMdDraft` 解析预填草稿（ElMessage 提示导入条数）。**md 规范格式（用户确认，写 md 必须遵守）**：每条 `### [类型] 模块 标题`（类型限 新增/修改/删除/修复）+ 标题下 `1.`/`-` 列表明细合并为描述；`## 会话 / ## 本次功能 / ## 待办 / ### 待办 / # 大标题 / > 说明` 均跳过（遇 `##`/`#` 关闭当前条目，防把待办并入上一条）
- `api/dev.ts`：`DevSummary` 含 `session`（当天进行中，无则 null）+ `sessions`（当天全部，时段卡用）；load 按日期去重逐个拉取各天功能
- **汇总 Tab（v1.27.0）**：数据卡顶部「明细 / 汇总」切换按钮；汇总视图 = RangeTabs（近 7 天 / 30 天 / 全部）+ 三指标卡（开发时长 / 开发会话数 / 功能条数）+ **ECharts 按天开发时长柱状图** + **按模块分布条形**（自绘 div，按条数降序）+ 按类型计数（新增/修改/修复/删除）；数据源 `GET /api/dev/statistics/range?days=`（days 缺省=全部，`api/dev.ts` 的 `DevRangeStats`：durationMinutes/sessionCount/featureCount/byDay/byModule/byType）；切范围 `watch(rangeKey)` 重载（首载带遮罩）；开发日志样式全部在 `devLog.scss`（嵌套 `.dev-log` 内 + 顶层 `.dev-log-detail`）

### 5.4 操作日志 `/operation-log`（views/operation-log/OperationLog.vue）
- header：模块/动作（中文）+ 日期范围筛选（服务端，**v1.23.0 起用 DateRangePicker 公共组件**）
- 列表列模式（时间/模块/动作/内容）+ 点击行弹详情；**v1.24.0**：时间90/模块110/动作90 固定宽完整显示 + 仅内容列省略（去除 v1.23.0 的 hideBelow 列隐藏）
- 分页：每页 15/30/50（`size="small"`），加载中不显示数量避免闪现
- **业务字典（v1.27.0 补 EXERCISE）**：模块枚举 `EXPENSE/WEIGHT/LEARN/USER/NOTE/EXERCISE`，中文映射 `记账/健康/学习/用户/笔记/锻炼`（`api/operationLog.ts` 的 `MODULE_LABELS`）；动作枚举 `CREATE/UPDATE/DELETE/RESTORE/LOGIN/REGISTER` → `新增/修改/删除/恢复/登录/注册`。锻炼模块（v1.26.0）操作日志用 `EXERCISE`，若映射缺失会显示裸英文

### 5.5 认证页 `/login`（views/login/）
- `LoginForm.vue` / `RegisterForm.vue` 表单区用**原生 `<form @submit.prevent="handleSubmit">`**，提交按钮 `BaseButton type="submit"`——**密码框内回车触发表单提交**（登录=登出、注册=注册）
- `FloatingInput`（浮动标签下划线输入，聚焦标签上移带展开动画）+ `BaseButton`（主色底白字 fw600）+ `AuthTitle`（左右两字分开，「录/册」字主色强调）+ `WallpaperCard`/`Fireworks`（登录成功烟花）

### 5.6 健康 `/health`（组合层 `views/health/Health.vue`）
> v1.16.0 起组件化拆分：组合层 `Health.vue` + `components/HealthInput`(打卡)/`HealthTrend`(趋势)/`HealthHistory`(历史+修改弹窗)；任一子页数据变更 `emit('changed')`→tick++→三处联动重载；目标体重经 userStore，趋势组件 `active` 时补渲染
- 布局：**内容区页头四子页**（`hl__subnav` Tab，v-show 切换）——**打卡 / 趋势 / 锻炼 / 历史**（v1.26.0 起加「锻炼」），`max-width:1500px` 居中；原型：`prototypes/health-proto/index.html`（网页版 + H5 双视图）
- **打卡子页**：大号等宽体重输入（kg）+ 可选体脂率%/腰围cm + 日期（默认今天可补记）+ 备注 → 保存（清空表单可连续补记）+ **体重速览**（最近 5 条，独立轻量加载 `recentLoading`，可跳历史页）
- **趋势子页**：四指标（当前体重/较上次变化[降绿升红]/距目标[accent 卡，还需减或已低于]/累计记录，由 trend 数据前端计算，目标体重取 `userStore.userInfo.targetWeight`）+ **ECharts 体重折线**（面积渐变 + 目标体重 markLine 虚线，日期 MM-DD 短标签、多时稀疏）+ 30/90/全部 范围切换（trend 全量拉回前端过滤，切范围不重复请求）+ **可收纳/展开动画**（grid-template-rows 0fr↔1fr）+ 下方**体脂率/腰围小趋势卡**（有数据才渲染 `hasFat/hasWaist`）
- **历史子页**：日期范围筛选（服务端，**v1.23.0 起用 DateRangePicker**）+ **DataList** 列表（v1.17.0 起统一用 DataList：日期/体重/体脂腰围备注/记录时间 + 操作；每页 15/30/50 联动可见行数）+ 修改弹窗（体重必填校验、体脂 0-100、腰围>0）+ 分页；点击行弹只读详情（日期/体重/体脂/腰围/记录时间/备注全字段）；**v1.24.0**：容器 `cardBelow:560` 卡片化降级——去 hideBelow、容器<560px 时整表变堆叠卡片、数据全展示不再省略（component `ops:true` 归卡片底部），健康页根 `min-width:450px`
- **图标**：Lucide `Weight/Percent/Ruler/Pencil/Trash2/ChevronUp/ChevronDown`
- **黑夜模式**：同记账，`cssVar()` 取实际色值 + `watchTheme()` 重绘；子页 v-show `clientWidth>0` 才 init；切到趋势子页/收纳展开完成(360ms)/范围切换/目标体重异步补齐 时重绘
- **加载分组**：趋势（指标+折线+小图共用 trend 接口）`trendLoading` 一个；速览 `recentLoading` 独立；历史 `listLoading` 独立；保存/修改/删除后 `refreshAll()` 联动三处
- 后端：`api/health.ts`（分页/趋势/打卡/改/删）；`GET /weight-records/trend` 已扩展返回 `bodyFats/waists` 序列
- **空态**：无记录时趋势页显示引导（Weight 图标 + 文案 + 去打卡按钮）

#### 5.6.1 锻炼子页（v1.26.0 新增）
- 健康页第 4 个 Tab「锻炼」，三个组件：`ExerciseInput`(打卡+今日列表) / `ExerciseStats`(统计+近14天图表) / `ExerciseHistory`(历史+修改弹窗)；`api/exercise.ts` 封装全部接口
- **数据模型**：动作字典 `exercise_item`（type: strength力量/plank平板/walk散步/stairs爬楼梯；基础 MET；参考速度 ref_speed）+ 锻炼记录 `exercise_record`（只存原始参数：重量/个数/分钟/距离/层数/次数/秒/手/备注）——**大卡不落库**，前端 `utils/exercise.ts` 按 MET 公式实时计算（公式迭代历史自动跟随）
- **消耗公式（utils/exercise.ts 纯函数）**：
  - 通用：净消耗(kcal) = (MET−1)×3.5×体重×分钟÷200；总消耗 = MET×3.5×体重×分钟÷200
  - 力量：个数÷时长（分钟+秒合并）→实际速度÷参考速度→强度系数(0.6~1.4)→MET=基础MET×系数（做快做慢影响消耗）；时长输入「分+秒」精确到秒，落库存总秒数
  - 散步：距离+分钟→平均速度→档位 MET（<3.2慢2.8 / <4.0正常3.0 / <4.8稍快3.5 / <5.6快4.3 / ≥5.6暴走5.0）
  - 爬楼梯：层数×次数（做了什么）+ 手动填写分钟/秒 → MET 8.0；平板支撑：秒数，MET 4.0
  - **体重取「最新一条体重记录」**，绝不用目标体重；无体重记录时打卡提示先记录
- **打卡交互**：动作 chip 分组（力量/核心 + 有氧 + 自定义）→ 上次记录带出（`GET /exercise/latest`）→ 按动作类型动态表单 → 实时显示强度/MET/净消耗预览 → 保存后今日已练列表更新（可删）
- **统计**：今日/本周/本月净消耗 + 连续锻炼天数 + 近 14 天净消耗柱状图（ECharts，今日青绿色）+ 动作消耗分布 Top5；`GET /exercise/statistics` 返回全量记录由前端聚合
- **自定义动作**：弹窗填名称/类型/基础 MET（散步类可 0 自动按速度定档），`POST /exercise/items`；默认动作由注册时后端复制（user_id=1 预置 7 个）
- **历史**：日期范围筛选 + DataList（日期/动作/参数/净消耗[总消耗小字]/操作）+ 修改弹窗（按类型字段）+ 行点击只读详情

### 5.7 学习 `/learn`（组合层 `views/learn/Learn.vue`）
> v1.16.0 起组件化拆分：组合层 `Learn.vue` + `components/LearnInput`(记一笔)/`LearnStats`(统计)/`LearnHistory`(历史+修改弹窗) + `learnShared.ts`(WAYS/durationParts 共享)；统计组件 `emit('stats-loaded', totalDuration)` 供 SubNav hint；任一子页数据变更 `emit('changed')`→tick++→三处联动重载
- 布局：**内容区页头三子页**（SubNav Tab，v-show 切换）——**记一笔 / 统计 / 历史**，`max-width:1500px` 居中；样式按用户确认的「学习模块三子页样式预览」落地（主色 #4f7a8c）
- **记一笔子页**：主题下划线输入（必填）+ **方式宫格**（阅读/视频/课程/实践/其他，Lucide 图标单选）+ 时长（等宽大输入，可空）+ **掌握程度 5 星**（Star 图标，琥珀色）+ 日期（默认今天可补记）+ 收获笔记 + **附件上传**（el-upload auto-upload=false，最多 5 个：图片/PDF/doc）+ 保存（清空可连续补记）+ **最近记录速览**（最新 5 条，独立轻量加载，可跳历史）
- **统计子页**：四指标（累计学习/今日时长/本月时长/掌握均分[accent]）+ **ECharts 近 14 天学习时长折线**（面积渐变 + 入场动画）+ **ECharts 按方式构成环形图**（计数，入场动画）+ **ECharts 掌握程度分布横向条形**（1-5★）；无记录时空态引导
- **历史子页**：日期范围（**v1.23.0 起用 DateRangePicker**）+ 方式下拉 + 关键词搜索（均服务端过滤）+ **DataList** 列表（v1.17.0 起统一用 DataList：主题[附件数]/方式/时长/掌握星/日期 + 操作）+ 修改弹窗（含已有附件可删 + 新增附件）+ 分页 15/30/50；**v1.23.0**：次要列（方式/时长/掌握星/日期）按 `hideBelow` 窄屏隐藏 + 点击行弹只读详情
- **附件**：`api/learn.ts` 上传 `POST /files/note`、删除 `DELETE /files/note/{id}`、列表 `GET /files/note/list`；访问地址 `/uploads/{filePath}`（vite 已代理）
- **图标**：Lucide `BookOpen/Clapperboard/GraduationCap/Wrench/Sparkles/Pencil/Trash2/Paperclip/Star/Timer/NotebookPen`
- **黑夜模式**：同记账/健康，`cssVar()` 取实际色值 + `watchTheme()` 重绘；子页 v-show `clientWidth>0` 才 init；切到统计子页补渲染
- **加载分组**：统计（指标+三图共用 statistics 接口）`statsLoading` 一个；速览 `recentLoading` 独立；历史 `listLoading` 独立；保存/修改/删除后 `refreshAll()` 联动三处
- 后端：`api/learn.ts`（分页/统计/增删改 + 附件三接口）；`GET /learn-records/statistics` 返回 `{totalCount,totalDuration,todayMinutes,monthMinutes,avgMastery,byWay,mastery,trend{dates,minutes}}`
- **无测试数据**：页面空态为主，不写入任何示例数据

### 5.8 个人中心 `/profile`（views/profile/Profile.vue，v1.25.0 重做版）
- 页面结构由「双卡表单平铺」改为**「概要卡 + 设置列表 + 弹窗编辑」**（类名前缀 `ppf__`，`max-width:1200px` 居中，设置区限宽 720px，根 `min-width:380px`）；概要卡沿用（头像/昵称/@用户名/加入时间/退出登录，**不展示手机号**——设置列表「修改手机号」行已有脱敏展示，避免重复）
- **设置列表 6 项**（每行图标+名称+右侧摘要+箭头，点击弹 el-dialog 编辑）：更换头像（弹窗选图预览+确认上传）/ 修改昵称（弹窗输入）/ 修改手机号（**锁定项**，点击 toast「二期接入短信验证码后开放」）/ 设置目标体重（弹窗输入+当前最新体重与距目标对比+清除目标） / **身体数据**（v1.26.0：年龄/身高/性别，BMR 二期用） / 修改密码（弹窗内冷却提示+原/新/确认）
- **目标体重**：`updateProfileApi({targetWeight})` 保存；**清除**走 `clearTargetWeight:true`（v1.25.0 后端新增，置空 target_weight）；保存后 `setUserInfo` 联动首页减重进度与健康「距目标」
- **改密一月冷却**不变（`passwordUpdatedAt` 为空=首次免限；改密成功清 token 跳登录）
- `api/auth.ts`：`updateProfileApi` 新增 `clearTargetWeight` 字段

### 5.9 其余模块
- 首页概览 `homepage/Overview.vue`（v1.25.0 落地，类前缀 `ov-`；四区块自上而下：**问候区**（时段问候+昵称、日期星期、今日四模块完成度徽标 `exact count`）→ **今日四指标**（MetricCard 复用：支出/收入/体重/学习，体重附较上次变化↓绿↑黄）→ **本月两卡**（左 收支三数+支出分类构成条 Top5；右 减重进度条（起始→目标）+ 本月学习时长，学习目标待「目标体系」接入）→ **快捷入口**（记一笔/体重打卡/记学习，`@lucide/vue` 图标））；响应式 >900px 四列双卡并排 / ≤900px 两列双卡堆叠 / ≤560px 单列；`api/overview.ts` 封装 `getOverview()`

### 5.10 响应式适配约定（v1.23.0，用户逐项确认）
- **内容页根 padding 统一**：各页根容器 padding 一律 `clamp(0.75rem, 2vw, 2rem)`（12~32px 随视口平滑缩放），与 PagePanel 统一尺度，消除「PagePanel 一层 + 页面自带一层」的双层 padding；页面留白由内部 `page-panel` 的 `padding: clamp(...)` 提供
- **页面最小宽度**：个人中心 380px / 健康 450px / 记账 485px / 学习 430px / 每日总结 520px / 开发日志 550px（用户指定）——窄于阈值时由布局壳 `.app-layout__side`（`overflow-x:auto`）横向滚动兜底（header 与内容同进同出）
- **日期范围选择**：一律用 `DateRangePicker` 公共组件（宽屏整体双日历 / 视口<620px 拆上下两个单日选择 + preventOverflow 防右端溢出）
- **列表响应式（v1.24.0 双策略，用户确认）**：① **卡片化降级**（DataList 表格型、内容被窄屏压迫时）——列模式表格在容器 < `cardBelow` 时整表变堆叠卡片，数据全展示不省略（如健康历史 `cardBelow:560`）；② **固定宽 + 内容省略**（日志类）——关键前置列（日期/类型/模块/时间/动作等）固定宽完整显示，仅内容列省略（如开发/操作日志）。`hideBelow` 列隐藏方案已从健康/开发日志/操作日志移除（保留给学习）。**非所有列表都卡片化**，分组紧凑行列表（如记账明细）保持现状
- **记账明细**：按日分组的紧凑行列表（图标+分类+备注省略+时间+金额+操作，带日期头与小计），**不做卡片化**（v1.24.0 确认），页根 `min-width:485px`

### 5.11 H5 移动端 / APP 二期架构（规划，2026-08-21 用户确认）
> 仅记入文档作为二期基准，尚未实现。原型：`prototypes/expense-split-proto/index.html`（默认 H5 视图）、`prototypes/health-proto/index.html`（健康，H5 视图已知**卡片横向溢出问题，落地 H5 时需修复**）。
- **信息架构**：底部 5 Tab —— **首页 / 记账 / 健康 / 学习 / 我的**
  - **首页**：今日/本月指标卡 + 快捷入口（概览/明细/存钱/总结）+ 今日动态
  - **记账**：记一笔（支出/收入切换 + 分类宫格 4 列 + 大金额输入 + 底部保存）+ 内部「最近记录 / 概览」小切页
  - **健康 / 学习**：各自独立 Tab（体重趋势 / 学习时长）
  - **我的**：每日总结 / 操作日志 / 开发日志 等低频功能入口 + 设置（深色模式 / 关于）
- **设计要点**：触控目标 ≥44px；底部 Tab 常驻（保存按钮拇指区）；趋势图竖屏下用横向条形（日期竖排）；日历移动端改纵向列表/横向翻页；`el-dialog` 改底部弹出 sheet；下拉刷新/上拉加载
- **与桌面对应**：桌面导航的记账/健康/学习/每日总结/日志/个人中心 → 移动端为「底部 Tab 高频域 + 我的里低频入口」两级结构

---

## 6. 请求封装（api/request.ts）
- `requestApi({ url, method, params, data, silent })`：自动带 `Authorization` token；统一解包 `Result`；非 200 弹 `ElMessage`（`silent=true` 静默，供导航栏取数）；401 跳登录

---

## 7. 版本规则与文档约定

### 7.1 版本号规则（用户强约定，初始 v1.0.0）
- **高级改动** → 主版本 +1：v1.0.0 → v2.0.0
- **中等改动** → 次版本 +1：v1.0.0 → v1.1.0
- **低级改动** → 修订 +1：v1.0.0 → v1.0.1
- 当前版本：`v1.14.0`；预计后续按实际改动级别升级
- 改版时必须同步：`src/config.ts` 的 `APP_VERSION`、前端设计文档版本

### 7.2 文档约定（用户强约定）
- 前端改动记入 **前端设计文档**；后端改动记入 **后端设计文档**；表结构改动记入 **database/数据库设计文档**
- **今日功能记录-*.md** 为开发过程临时记录，**导入数据库（开发日志功能）后即删除**，不留长期文件
- 修改数据/代码后，须同步上述对应文档