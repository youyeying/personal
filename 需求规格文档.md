# 个人记录系统（Personal Record）需求规格文档

> 版本：v0.1.0（需求草案）
> 技术栈：前端 Vue3 + TypeScript ｜ 后端 Spring Boot 4.1
> 定位：个人自用的生活数据记录与可视化系统

---

## 1. 项目概述

### 1.1 背景

个人日常生活中产生的数据（花了多少钱、体重变化、每天学了什么）分散在手机备忘录、记账 App、手账里，难以统一查看和复盘。本项目构建一个**个人自用**的记录系统，把三类核心数据数字化、结构化、可视化：

1. **记账**：花销 / 收入，钱花在哪、赚了多少
2. **健康**：减肥过程，体重变化趋势
3. **学习**：每天学了什么，形成成长轨迹

### 1.2 目标

- 每天 1 分钟内完成各模块的记录，成本足够低
- 首页一屏看清「今日 + 本月」全貌
- 通过趋势图表帮助复盘：钱、身体、学习三方面的长期变化

### 1.3 使用场景

- 每天睡前花 1 分钟记账、记录体重、记录今天学的知识
- 月末复盘：本月花销结构、减重进度、学习时长
- 移动端（手机浏览器）为主，桌面端可用
- 后续目标：扩展为多用户、打包为 App（后端接口保持 RESTful 兼容）

---

## 2. 用户角色与权限

| 角色 | 说明 |
| --- | --- |
| 本人（初始用户） | 一期仅本人使用，全部功能可用 |
| 普通用户（二期扩展） | 注册后使用，数据按用户隔离 |

- 系统保留**登录认证**（JWT），原因：数据是隐私数据，且后续可在不同设备访问
- 一期权限模型从简：单一用户，不做角色分级
- 数据模型预留 `user_id` 字段与唯一约束，为多用户扩展打基础

---

## 3. 功能需求

### 3.1 记账模块（收支管理）

#### 3.1.1 收支记录
- **新增记录**：类型（支出 / 收入）、金额、分类、日期、备注
- **分类体系**：
  - 支出：餐饮、交通、购物、居住、娱乐、医疗、学习、人情、其他
  - 收入：工资、副业、理财、红包、其他
  - 分类**可自定义**（增删改），默认预置常用分类
- **查询**：按日期范围、分类、类型筛选；支持分页
- **操作**：编辑、删除（软删除）

#### 3.1.2 统计看板
- 本月总支出、总收入、结余
- 本月支出分类占比（饼图）
- 近 6 个月收支趋势（折线图，支出 / 收入双线）
- 今日支出 / 收入快捷查看

#### 3.1.3 数据要求
- 金额保留两位小数
- 日期精确到「天」，允许补记历史日期

### 3.2 健康模块（减肥记录）

#### 3.2.1 每日打卡
- 记录：日期、体重（kg）、体脂率（可选）、腰围（可选）、备注
- 支持补记历史日期
- 展示最近一条较上一条的变化（↓ / ↑ / —）

#### 3.2.2 目标管理
- 设置**目标体重**
- 展示进度：已减 / 还需减、完成百分比、BMI 数值

#### 3.2.3 统计看板
- 体重趋势折线图（近 30 天 / 全部）
- 打卡日历（按月，有记录的日期打点）

### 3.3 学习模块（学习记录）

#### 3.3.1 学习记录
- 新增：日期、主题 / 内容、时长（分钟）、学习方式（阅读 / 视频 / 课程 / 实践 / 其他）、收获笔记、掌握程度（1~5 星）
- **附件**：支持上传 PDF / 截图 / 文档，存本地磁盘，同一记录可挂多个附件
- 查询：按日期范围、方式、关键词筛选；支持分页
- 操作：编辑、删除

#### 3.3.2 统计看板
- 今日 / 本月学习时长
- 近 6 个月学习时长柱状图
- 学习方式占比（饼图）

### 3.4 首页概览

一屏汇总三大模块的「今日 + 本月」关键指标：

| 区域 | 内容 |
| --- | --- |
| 今日卡片 | 今日支出、今日收入、今日体重、今日学习时长 |
| 本月卡片 | 总支出 / 总收入 / 结余、减重进度、学习总时长 |
| 快捷入口 | 记一笔、体重打卡、记学习 |

### 3.5 系统功能
- 登录 / 登出（JWT + BCrypt）
- 个人资料：昵称、头像（可选）、目标体重（在健康模块设置）
- 响应式布局：桌面 / 平板 / 手机

---

## 4. 扩展想法（待确认）

以下为建议扩展点，按价值 / 成本排列，**是否纳入由用户决定**：

### 建议纳入二期（价值高、成本低）
| # | 功能 | 说明 |
| --- | --- | --- |
| 1 | 每日总结 | 每天一句话日记（心情 / 今日小结），配合三大记录形成完整时间线，回头翻看价值极高 |
| 2 | 统一日历视图 | 按月展示哪些天有记账 / 体重 / 学习记录，点击进入当天详情 |
| 3 | 目标体系 | 除减肥目标外，支持设月度预算、月度学习时长目标，首页展示完成进度 |
| 4 | 数据导出 | 月度账单 / 学习记录导出 Excel，便于存档与进一步分析 |

### 可选纳入三期（依赖通知渠道 / 使用习惯，成本高）
| # | 功能 | 说明 |
| --- | --- | --- |
| 5 | 习惯打卡 | 喝水 / 早睡 / 阅读等小习惯打卡，与健康模块合并为「生活方式管理」 |
| 6 | 每日提醒 | 固定时间提醒记账 / 打卡（依赖推送渠道，个人部署实现麻烦） |
| 7 | 历史数据导入 | 从 Excel 批量导入旧账目 / 旧体重数据（弥补前期未使用产生的空档） |
| 8 | 周报 / 月报 | 自动生成周 / 月复盘小结（文字 + 图表） |

### 一期暂不做
- 多用户开放注册、多人共享、云端同步
- App 打包（先用浏览器 PWA 方式使用；后端接口按 RESTful 设计，为后续 App 对接预留能力）

---

## 5. 非功能需求

- **性能**：个人使用数据量小；列表接口分页；统计接口数据低频变动可加缓存
- **安全**：密码 BCrypt 加密；接口 JWT 鉴权；全部数据软删除，误删可恢复
- **可维护性**：后端 Controller → Service → Mapper 分层；统一响应结构 `{ code, message, data }`、统一异常处理；前端按模块拆组件，样式用 SCSS 变量 / 主题令牌统一管理
- **可用性**：响应式覆盖手机 / 平板 / 桌面；记录表单步骤少、默认值合理（如日期默认今天）
- **文档同步**：需求、数据库设计、前后端说明文档随代码同步更新

---

## 6. 技术方案

### 6.1 前端（E:/personal/front）
| 项 | 选型 |
| --- | --- |
| 框架 | Vue3 + TypeScript（`<script setup lang="ts">`） |
| 构建 | Vite |
| 状态管理 | Pinia |
| 路由 | Vue Router |
| UI 组件 | Element Plus |
| 图表 | ECharts |
| 请求 | Axios（统一封装，自动携带 Token，统一错误提示） |

### 6.2 后端（E:/personal/backend）
| 项 | 选型 |
| --- | --- |
| 框架 | Spring Boot 4.1（Java 26） |
| ORM | MyBatis-Plus |
| 数据库 | MySQL |
| 认证 | JWT（拦截器统一校验） |
| 密码 | BCrypt |

### 6.3 目录规划（草案）
```
E:/personal
├── backend/                # Spring Boot 后端
│   └── src/main/java/com/personal/backend/
│       ├── controller/     # 接口层
│       ├── service/        # 业务层
│       ├── mapper/         # 数据访问层
│       ├── entity/         # 实体
│       ├── dto/            # 请求 / 响应对象
│       ├── config/         # 配置（JWT、拦截器、跨域）
│       └── common/         # 统一响应、异常、工具
└── front/                  # Vue3 前端
    └── src/
        ├── api/            # 接口请求
        ├── types/          # TS 类型
        ├── store/          # Pinia
        ├── router/         # 路由
        ├── views/          # 页面
        ├── components/     # 通用组件
        ├── styles/         # 全局样式
        └── utils/          # 工具函数
```

### 6.4 文件存储方案
- 所有文件（头像、学习附件）存**本地磁盘** `uploads/` 目录，数据库只存**相对路径**
- Spring Boot 配置静态资源映射，将 `uploads/` 暴露为 `/uploads/**`，前端直接拼 URL 回显
- 目录结构：
  ```
  uploads/
  ├── avatar/            # 头像
  └── note/              # 学习附件（按 年/月/日 分目录）
  ```
- 文件名用 UUID，防重名冲突；上传校验类型与大小（图片 ≤ 2MB）

---

## 7. 数据库设计草案

> 全部业务表软删除（`deleted` 字段），`created_at` / `updated_at` 审计字段。
> 多用户系统：业务表统一带 `user_id` 做数据隔离；一期仅注册一个初始用户。

### 7.1 user（用户表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| username | VARCHAR(30) | 登录名，1-30 位，唯一 |
| password | VARCHAR(100) | 密码哈希（明文密码 8-16 位） |
| phone | VARCHAR(11) | 手机号，11 位，唯一 |
| nickname | VARCHAR(20) | 昵称，1-20 位 |
| avatar | VARCHAR(255) | 头像本地相对路径（可选） |
| target_weight | DECIMAL(5,2) | 目标体重（kg） |
| created_at / updated_at | DATETIME | 审计 |
| deleted | TINYINT | 软删除 |

### 7.2 expense_category（收支分类表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户（多用户隔离） |
| name | VARCHAR(20) | 分类名，1-20 位 |
| type | TINYINT | 1 支出 / 2 收入 |
| sort_order | INT | 排序 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

- 唯一约束 `(user_id, name, type, deleted)`：同一用户下同类分类名不重复
- 预置默认分类（user_id=1）：支出 9 个 + 收入 5 个；新用户注册后由后端复制默认分类

### 7.3 expense_record（收支记录表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户 |
| type | TINYINT | 1 支出 / 2 收入 |
| category_id | BIGINT | 分类 |
| amount | DECIMAL(10,2) | 金额 |
| note | VARCHAR(255) | 备注 |
| record_date | DATE | 记录日期 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

### 7.4 weight_record（体重记录表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户 |
| weight | DECIMAL(5,2) | 体重（kg） |
| body_fat | DECIMAL(4,2) | 体脂率（可选） |
| waist | DECIMAL(4,2) | 腰围 cm（可选） |
| note | VARCHAR(255) | 备注 |
| record_date | DATE | 记录日期 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

### 7.5 learn_record（学习记录表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户 |
| title | VARCHAR(100) | 学习主题 |
| content | TEXT | 收获笔记 |
| duration | INT | 时长（分钟） |
| way | VARCHAR(20) | 方式：阅读/视频/课程/实践/其他 |
| mastery | TINYINT | 掌握程度 1~5 |
| learn_date | DATE | 学习日期 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

### 7.6 note_file（学习附件表，一期）
> 学习记录关联的 PDF / 截图等附件。文件存本地 uploads/note/ 目录，数据库只存路径。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户 |
| learn_record_id | BIGINT | 所属学习记录 |
| file_name | VARCHAR(255) | 原始文件名 |
| file_path | VARCHAR(255) | 本地相对路径（含 UUID 文件名） |
| file_type | VARCHAR(20) | 类型：pdf / png / jpg |
| file_size | BIGINT | 文件大小（字节） |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

### 7.7 daily_note（每日总结表，二期）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 所属用户 |
| note_date | DATE | 日期（唯一） |
| mood | VARCHAR(20) | 心情 |
| content | TEXT | 今日小结 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

### 7.8 operation_log（操作日志表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 主键 |
| user_id | BIGINT | 操作者 |
| module | VARCHAR(20) | 操作模块：EXPENSE / WEIGHT / LEARN / USER / NOTE |
| action | VARCHAR(20) | 操作动作：CREATE / UPDATE / DELETE / RESTORE / LOGIN / REGISTER |
| target_id | BIGINT | 操作对象 ID |
| content | VARCHAR(500) | 操作内容描述 |
| created_at / updated_at / deleted | - | 审计 + 软删除 |

---

## 8. 页面清单

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| /login | 登录页 | 用户名 + 密码 |
| / | 首页概览 | 今日 + 本月三大模块汇总 + 快捷入口 |
| /expense | 记账列表 | 收支记录增删改查 + 筛选 |
| /expense/stat | 记账统计 | 收支汇总、分类占比、趋势 |
| /health | 健康管理 | 体重打卡 + 趋势 + 目标进度 |
| /learn | 学习记录 | 学习记录增删改查 + 统计 |
| /profile | 个人中心 | 昵称、密码、目标体重 |

---

## 9. 里程碑规划

### 一期（核心闭环）
1. 项目脚手架：后端分层 + JWT 登录、前端框架 + 布局 + 路由守卫
2. 记账模块：增删改查 + 分类 + 统计
3. 健康模块：体重打卡 + 趋势 + 目标
4. 学习模块：增删改查 + 统计
5. 首页概览 + 响应式适配

### 二期（增强体验）
1. 每日总结 + 统一日历视图
2. 目标体系（月度预算 / 学习时长目标）
3. 数据导出 Excel

### 三期（按需）
习惯打卡、每日提醒、历史数据导入、周报 / 月报
