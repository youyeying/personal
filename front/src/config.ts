/**
 * 应用全局配置
 */

/**
 * 版本号规则（用户约定，初始 v1.0.0）：
 * - 高级改动 → 更新第一个数字（主版本）：v1.0.0 → v2.0.0
 * - 中等改动 → 更新第二个数字（次版本）：v1.0.0 → v1.1.0
 * - 低级改动 → 更新第三个数字（修订）  ：v1.0.0 → v1.0.1
 */
export const APP_VERSION = '2.1.0'

/** 应用名 */
export const APP_NAME = '个人记录'