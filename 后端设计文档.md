# 个人记录系统 后端设计文档

> 版本：v1.4.0（2026-08-29 开发日志范围汇总接口 + 操作日志模块补 EXERCISE）
> 技术栈：Spring Boot 4.1 + MyBatis-Plus 3.5.17 + MySQL 8.0 + JWT + BCrypt
> 服务端口：8080 ｜ 数据库：personal_record
> 默认账号：admin / admin123（仅空库首次启动自动创建，仓库展示已脱敏）
>
> 相关文档：前端设计文档 → `E:/personal/前端设计文档.md` ｜ 数据库设计文档 → `E:/personal/database/数据库设计文档.md`

---

## 1. 项目结构

```
backend/
├── pom.xml                        # Maven 依赖与构建
└── src/main/java/com/personal/backend/
    ├── BackendApplication.java    # 启动类（@MapperScan 扫描 mapper）
    ├── common/                    # 通用基础设施
    │   ├── Result.java            # 统一响应结构 { code, message, data }
    │   ├── BizException.java      # 业务异常
    │   ├── GlobalExceptionHandler.java  # 全局异常处理
    │   ├── LoginUser.java         # 当前登录用户信息（id + username）
    │   └── UserContext.java       # 登录用户上下文（ThreadLocal）
    ├── config/                    # 配置类
    │   ├── WebConfig.java         # 拦截器注册、跨域、静态资源映射
    │   ├── MybatisPlusConfig.java # 分页插件
    │   ├── MybatisPlusMetaHandler.java  # 审计字段自动填充
    │   └── DataInitializer.java   # 启动时创建初始账号
    ├── interceptor/
    │   └── AuthInterceptor.java   # JWT 认证拦截器（含白名单）
    ├── utils/
    │   └── JwtUtils.java          # JWT 生成/解析
    ├── entity/                    # 实体类（11 张表）
    ├── mapper/                    # MyBatis-Plus Mapper（11 个）
    ├── dto/                       # 请求/查询参数（10 个）
    ├── service/                   # 业务逻辑（11 个）
    └── controller/                # REST 接口（11 个）
```

---

## 2. 技术选型与版本

| 组件 | 版本 | 说明 |
| --- | --- | --- |
| Spring Boot | 4.1.0 | 基础框架（Java 26） |
| MyBatis-Plus | 3.5.17 | `mybatis-plus-spring-boot4-starter` + `mybatis-plus-jsqlparser` |
| MySQL | 8.0.46 | `mysql-connector-j` |
| JWT | jjwt 0.12.6 | Token 认证 |
| 密码 | spring-security-crypto | BCrypt 加密 |
| Lombok | 1.18.46 | 简化实体（需在 maven-compiler-plugin 显式声明注解处理器） |

---

## 3. 设计约定

### 3.1 统一响应结构
所有接口返回 `Result<T>`，格式：
```json
{ "code": 200, "message": "操作成功", "data": { ... } }
```
- `code=200` 成功；`400` 参数/业务错误；`401` 未登录/过期；`500` 系统异常

### 3.2 认证机制
- 登录/注册签发 JWT，客户端请求头带 `Authorization: Bearer <token>`
- `AuthInterceptor` 拦截 `/api/**`，白名单 `/api/auth/login`、`/api/auth/register` 免认证
- 拦截器解析 token 后把当前用户放入 `UserContext`，业务层通过 `UserContext.requireUserId()` 取当前用户

### 3.3 数据隔离
- 所有业务表带 `user_id`，查询/操作均限定当前登录用户，防越权
- 实体 `deleted` 字段标注 `@TableLogic`，MyBatis-Plus 自动软删除过滤

### 3.4 审计字段
- 实体统一 `createdAt` / `updatedAt`，由 `MybatisPlusMetaHandler` 自动填充

### 3.5 权限
- 单用户系统（初始账号 admin，接口权限从简，不做角色分级）

---

## 4. 数据库 10 张表（实体）

| # | 表 | 实体 | Mapper | 说明 |
| --- | --- | --- | --- | --- |
| 1 | user | User | UserMapper | 用户 |
| 2 | expense_category | ExpenseCategory | ExpenseCategoryMapper | 收支分类 |
| 3 | expense_record | ExpenseRecord | ExpenseRecordMapper | 收支记录 |
| 4 | weight_record | WeightRecord | WeightRecordMapper | 体重记录 |
| 5 | learn_record | LearnRecord | LearnRecordMapper | 学习记录 |
| 6 | note_file | NoteFile | NoteFileMapper | 学习附件 |
| 7 | daily_note | DailyNote | DailyNoteMapper | 每日总结 |
| 8 | operation_log | OperationLog | OperationLogMapper | 操作日志 |
| 9 | development_session | DevelopmentSession | DevelopmentSessionMapper | 开发会话 |
| 10 | feature_log | FeatureLog | FeatureLogMapper | 功能变更记录 |

> 表结构详见 `database/数据库设计文档.md`

---

## 5. 接口清单

### 5.1 认证模块 `/api/auth`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /auth/register | 注册（复制默认分类 + 记日志） |
| POST | /auth/login | 登录（签发 token + 记日志） |
| GET | /auth/me | 获取当前用户信息 |
| PUT | /auth/profile | 修改个人信息（昵称/目标体重/头像；本期不允许改手机号，二期验证码开放）。**v1.2.0 新增 `clearTargetWeight` 布尔**：为 true 时置空目标体重（id 字段为 null 时忽略，需清除时传此字段）。**v1.3.0 新增 `age/height/gender`** 身体数据（BMR 二期用，gender 限 male/female） |
| PUT | /auth/password | 修改密码（一月冷却：`password_updated_at` 为空=首次免限，改后开始计冷却，距上次<1月拒绝） |

### 5.2 开发会话与功能变更 `/api/dev`（开发最先实现）
> v1.19.0：支持**一天多段会话**（可多次开始/结束）。`development_session` 去掉 `session_date` 唯一键（同日多条=多段）；`start()` 有进行中则幂等返回、无则新建当天一段；`end()` 结束当前进行中一段；`summary()` 当天多段时长累计 + 返回 `sessions`（当天全部会话）。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /dev/session/start | 开始开发（有进行中则幂等返回，否则新建当天一段） |
| POST | /dev/session/end | 结束开发（结束当前进行中一段，自动计算本段时长） |
| POST | /dev/features | 记录功能变更（新增/修改/删除/修复，需进行中会话） |
| GET | /dev/summary?date= | 当天统计（多段时长累计 + 各类型功能数 + 明细 + sessions 当天全部会话） |
| GET | /dev/statistics | 历史统计（会话列表 + 总时长 + 总功能数） |
| GET | /dev/statistics/range?days= | **v1.4.0 范围汇总**（days 缺省=全部）：`{durationMinutes, sessionCount, featureCount, byDay{date:分钟}, byModule{模块:条数}, byType{类型:条数}}`；进行中会话不计入 |

### 5.3 操作日志 `/api/operation-logs`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /operation-logs | 分页查询当前用户操作日志 |

### 5.4 记账模块
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /expense-categories?type= | 分类列表 |
| POST | /expense-categories | 新增分类 |
| PUT | /expense-categories/{id} | 修改分类 |
| DELETE | /expense-categories/{id} | 删除分类 |
| GET | /expense-records | 收支记录分页 |
| GET | /expense-records/statistics | 收支统计 |
| POST | /expense-records | 新增收支 |
| PUT | /expense-records/{id} | 修改收支 |
| DELETE | /expense-records/{id} | 删除收支 |

### 5.5 健康模块
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /weight-records | 体重记录分页 |
| GET | /weight-records/trend | 体重趋势（日期升序，含 weights/bodyFats/waists 序列，2026-08-22 扩展） |
| POST | /weight-records | 体重打卡 |
| PUT | /weight-records/{id} | 修改 |
| DELETE | /weight-records/{id} | 删除 |

### 5.5.1 锻炼模块（v1.3.0 新增，动作字典 + 记录）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /exercise/items | 我的锻炼动作列表 |
| POST | /exercise/items | 新增自定义动作（name/type/baseMet/refSpeed/hasWeight/hasHand） |
| PUT | /exercise/items/{id} | 修改动作 |
| DELETE | /exercise/items/{id} | 删除动作（软删，不影响已有记录） |
| GET | /exercise | 锻炼记录分页（startDate/endDate/exerciseId/page/size） |
| GET | /exercise/statistics | 统计原始记录（全量，前端按 MET 公式聚合今日/本周/本月/连续/趋势） |
| GET | /exercise/latest?exerciseId= | 某动作最近一条记录（打卡页「上次」带出） |
| POST | /exercise | 新增锻炼记录（按动作类型校验必填字段） |
| PUT | /exercise/{id} | 修改锻炼记录 |
| DELETE | /exercise/{id} | 删除锻炼记录 |

**设计要点**：消耗大卡/MET 由**前端 `utils/exercise.ts` 按体重计算，后端不落库**（公式迭代历史自动跟随）；体重取最新一条 weight_record，绝不用目标体重；记录表只存原始参数。注册时后端复制 user_id=1 的默认动作到新用户。

### 5.6 学习模块
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /learn-records | 学习记录分页（含附件） |
| GET | /learn-records/statistics | 学习统计：`{totalCount,totalDuration,todayMinutes,monthMinutes,avgMastery,byWay(计数),mastery(1-5分布),trend{dates,minutes}(近14天)}`（2026-08-23 扩展） |
| POST | /learn-records | 新增 |
| PUT | /learn-records/{id} | 修改 |
| DELETE | /learn-records/{id} | 删除（级联软删附件） |

### 5.7 文件上传 `/api/files`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /files/note | 上传学习附件（PDF/图片/文档） |
| DELETE | /files/note/{id} | 删除附件（物理删文件） |
| GET | /files/note/list?learnRecordId= | 某记录的附件列表 |
| POST | /files/avatar | 上传头像 |

### 5.8 每日总结 `/api/daily-notes`（二期功能）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /daily-notes/date?date= | 某天总结 |
| POST | /daily-notes | 保存总结 |
| GET | /daily-notes | 按日期范围查列表 |

### 5.9 首页概览 `/api/overview`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /overview | 今日 + 本月三大模块汇总 |

返回结构（`{ today, month }`）：
- **today**：`expense`/`income`（今日收支）、`expenseCount`/`incomeCount`（笔数）、`weight`（今日体重，可空）、`learnMinutes`/`learnCount`（今日学习）、`done`（四模块完成度 `{expense, weight, learn, note}`，用于首页「今日完成度」徽标）
- **month**：`expense`/`income`/`balance`、`learnMinutes`、`latestWeight`/`previousWeight`（最近两次体重，供「较上次变化」）、`startWeight`（最早一条体重，减重起点）、`targetWeight`（`user.target_weight`，可空）、`expenseCategories`（本月支出分类构成，金额降序 Top5 后其余合为「其他」，`[{name, amount}]`）

---

## 6. 核心业务逻辑

### 6.1 注册
1. 校验用户名/手机号唯一
2. BCrypt 加密密码，插入 user
3. 复制 user_id=1 的默认分类到新用户
4. 记操作日志（REGISTER），签发 token

### 6.2 登录
1. 按用户名查用户（自动过滤软删除）
2. BCrypt 校验密码
3. 记操作日志（LOGIN），签发 token

### 6.3 开发会话工作流
- `start()`：当天无会话则新建（status=0）；已结束则报错
- `end()`：计算 `start_time` 到当前时间的分钟数（不足 1 分钟按 1 计），status=1
- `addFeature()`：需有进行中的会话，记录类型（新增/修改/删除/修复）
- `summary()`：当天时长（未结束则实时算）+ 功能明细 + 各类型数量
- `statistics()`：历史会话 + 总时长 + 总功能数

### 6.4 记账
- 金额 DECIMAL(10,2)，校验 > 0
- 分类归属校验（防越权），分类类型与收支类型匹配
- 统计：按日期范围汇总支出/收入/分类汇总

### 6.5 体重
- 体重 > 0 校验，同日允许多条（早晚各一）
- trend() 返回日期 + 体重 + 体脂 + 腰围序列供折线图（体脂/腰围可为 null）

### 6.6 学习
- 主题必填、方式白名单（阅读/视频/课程/实践/其他）、掌握程度 1-5
- 附件一对多（note_file），删除记录级联软删附件

### 6.7 文件存储
- 文件存本地 `uploads/` 目录，数据库只存相对路径
- 学习附件：`note/yyyy/MM/dd/{uuid}.ext`，图片/附件校验类型与大小
- 头像：`avatar/{userId}/{uuid}.ext`，≤2MB
- Spring 静态资源映射 `/uploads/**` → 磁盘目录

### 6.8 操作日志
- `OperationLogService.record(module, action, targetId, content)`
- 登录/注册（无 UserContext）用 `record(userId, ...)` 显式指定
- 其他业务操作取当前登录用户

### 6.9 通用工具（utils/，v1.17.0 起消除重复样板）
- **`OwnedUtil.requireOwned(mapper, id, userId, userIdGetter, errMsg)`**：数据归属校验（存在且属于当前用户才返回，否则抛 `BizException`）；替代原先散落在 5 个 Service（Weight/Expense/ExpenseCategory/Learn/File）里重复的「selectById + getUserId 判断 + 抛异常」
- **`PageUtil.ok(page, records)`**：分页响应组装 `{records,total,current,size}`；替代 Weight/Expense/OperationLog/Learn 四个 Service 重复的 `Map.of(...)`

---

## 7. 关键配置

### 7.1 application.properties
| 配置 | 值 |
| --- | --- |
| server.port | 8080 |
| spring.datasource.url | jdbc:mysql://localhost:3306/personal_record |
| mybatis-plus 逻辑删除 | deleted: 0 未删 / 1 已删 |
| app.jwt.secret | personal-record-jwt-secret-key-... |
| app.jwt.expire-hours | 24 |
| app.upload-path | ./uploads |

### 7.2 pom.xml 关键点
- Spring Boot 4 + Java 26 下，Lombok 需在 `maven-compiler-plugin` 显式配置 `annotationProcessorPaths`
- MyBatis-Plus 3.5.9+ 分页插件拆分，需额外依赖 `mybatis-plus-jsqlparser`

---

## 8. 已解决问题记录

| 问题 | 根因 | 解决 |
| --- | --- | --- |
| Lombok getter 找不到 | Java 26 不自动发现注解处理器 | pom 配置 annotationProcessorPaths |
| PaginationInnerInterceptor 找不到 | 3.5.9+ 拆分独立依赖 | 加 mybatis-plus-jsqlparser |
| 启动报 utf8mb4 编码错误 | JDBC URL characterEncoding 不支持 utf8mb4 | 改为 utf8 |
| 登录返回 401 | 登录时记录操作日志调用 requireUserId，UserContext 为空 | 登录/注册显式传 userId |

---

## 9. 启动方式

```bash
# 前端构建产物生成后，后端单独启动
cd E:/personal/backend
.\mvnw.cmd spring-boot:run
# 或打包后运行
.\mvnw.cmd clean package -DskipTests
java -jar target/backend-0.0.1-SNAPSHOT.jar
```

首次启动 `DataInitializer` 自动创建账号 `admin / admin123`。

---

## 10. 新增（2026-08-20）

### 10.1 班表模块 `/api/shifts`（新表 shift_record）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /shifts/batch | 批量保存一个班期（每月21日→次月20日），body `{startDate, endDate, shifts:[{date, shiftName, note?}]}`，按 (user,date) upsert |
| GET | /shifts?startDate&endDate | 按日期范围查班表（升序） |

- 表 `shift_record`：user_id / shift_date（同日唯一）/ shift_name（班次名）/ note，软删除+审计。
- 新增文件：entity/ShiftRecord、mapper/ShiftRecordMapper、service/ShiftService、controller/ShiftController、dto/ShiftBatchRequest。
- **班次类型（shift_name 约定值，前端 SHIFT_META 提供短标签/着色，后端不强制枚举）**：早上7-16、早上7点半-16点30、早上10-19、中午12-21、下午15-24、下午17-第二天2、23-第二天8、休息。

### 10.2 每日总结新增小汇总
- `GET /api/daily-notes/summary?date=`：当日小汇总（支出/收入、体重+较上次变化、学习条数/时长、开发时长），由 `DailyNoteService.summary()` 聚合 expense/weight/learn/development_session。

### 10.3 操作日志日期筛选
- `OperationLogQuery` 新增 `startDate/endDate`，`OperationLogService.page()` 按 `createdAt` 过滤（结束日取次日 00:00 前）。
